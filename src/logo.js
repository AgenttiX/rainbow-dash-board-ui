/* eslint-disable max-len */
export default ({ width, height }) => {
  return (
    <img
      src="./images/logo.svg"
      alt="Rainbow Dash Board"
      width={width}
      height={height}/>
  );
};
